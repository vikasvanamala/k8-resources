apiVersion: v1
kind: Pod

metadata:
  name: service-np-demo
  # These labels are used to register this pod with service
  labels:
    project: roboshop
    component: frontend
    environemnet: dev
    purpose: node-port

spec:
  containers:
  - name: nginx
    image: nginx
