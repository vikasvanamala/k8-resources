apiVersion: v1
kind: Service

metadata:
  name: nginx-lb

spec:
  type: LoadBalancer
  selector:
    project: roboshop
    environment: dev
    component: frontend
    purpose: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80